// for each loop considered as method for array than a loop

const myStates = [
    "Karnataka", 
    "Telangana",
    1985, 
    "Kerala", 
    1999,
    "Maharastra"
];

//insted of for loop for array
myStates.forEach( (s) => (console.log(s)));

myStates.forEach((s) => console.log(s));

myStates.forEach(s => console.log(s));