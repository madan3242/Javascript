const color = ['red', 'green', 'blue', 'orange', 'yellow'];

let last = color[color.length-1];
console.log(`Last element = ${last}`);

let lastEle = color.slice(-1);
console.log(`Last element = ${lastEle}`);